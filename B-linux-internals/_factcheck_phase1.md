# Appendix B · linux-internals — factcheck (Phase 1)

> Reference appendix (deep info only, NO exercises — CONSTITUTION #5). Verifies the load-bearing
> claims of B against **line-verified spine canon** — primarily **04** (which cited xv6 + OSTEP +
> TLPI/man-pages + the Linux kernel docs/source: `sched.h` CFS/EEVDF, cgroup v2, procfs, `epoll(7)`,
> bpf verifier) plus **A** (TLB/page walk), **13** (latency), **N** (math). **NO new primary fetched
> this wave** — man7.org / kernel.org docs HTTP **000** (re-checked Wave 18). Every quantitative claim
> re-derived in `_recompute.py` (14/14). Blockers: **0**.

## Claim ledger

| # | Claim | Status | Source / basis |
|---|-------|--------|----------------|
| 1 | Linux has ONE task abstraction; `fork`/`pthread_create` both call **clone()**; thread vs process = CLONE_* flags | RECOMPUTED + VERIFIED (reuse) | `_recompute.py` #1; 04 §1 "threads as clone-created tasks sharing VM/files/signals" |
| 2 | **COW fork**: 0 pages copied at fork; only written pages fault-and-copy; enables cheap fork+exec + overcommit | RECOMPUTED + VERIFIED (reuse) | `_recompute.py` #2; 04 §1 "Linux uses COW in practice", §3 overcommit |
| 3 | **CFS**: smallest-vruntime (RB-tree leftmost, O(log n)) → n equal tasks get 1/n CPU; nice = weight (~1.25×/level) | RECOMPUTED + VERIFIED (reuse) | `_recompute.py` #3; 04 §1 "CFS orders tasks by virtual runtime in an RB-tree" |
| 4 | **EEVDF** (6.6+ default): adds virtual deadline; smaller requested slice → earlier deadline → lower latency | RECOMPUTED + VERIFIED (reuse) | `_recompute.py` #4; 04 §1 "EEVDF with lag and virtual deadlines for latency-sensitive work" |
| 5 | **Page cache**: MemFree is misleading; MemAvailable counts reclaimable cache ("Linux ate my RAM" myth) | RECOMPUTED + VERIFIED (reuse) | `_recompute.py` #5; 04 §1 "MemFree vs MemAvailable", misconception list |
| 6 | **Buddy allocator**: power-of-2 page orders (split/merge); slab/kmalloc caches cut small-object fragmentation | RECOMPUTED + VERIFIED (reuse) | `_recompute.py` #6; 04 §1 "buddy allocation … slab/kmem caches" |
| 7 | **epoll** is O(ready) not O(watched) — the C10K fix; ET requires non-blocking drain-to-EAGAIN | RECOMPUTED + VERIFIED (reuse) | `_recompute.py` #7; 04 §1 "epoll scales … interest and ready lists", §3 ET drain rule |
| 8 | **io_uring**: batches N ops into ~1 syscall via shared SQ/CQ rings; beats epoll+read for high IOPS | RECOMPUTED | `_recompute.py` #8; 04 §6 lists io_uring as appendix-B topic (concept; exact ring layout `[UNVERIFIED]`) |
| 9 | **cgroups v2**: `cpu.max` = quota/period fractional CPU cap; `memory.max` = hard cap → per-cgroup OOM | RECOMPUTED + VERIFIED (reuse) | `_recompute.py` #9; 04 §2 (cgroup v2 docs cited); substrate for appendix I |
| 10 | **Namespaces**: a container = a process in N composable namespaces (pid/net/mnt/uts/ipc/user/cgroup/time) | RECOMPUTED + VERIFIED (reuse) | `_recompute.py` #10; 04 §6 "namespaces/containers … appendix B/I" |
| 11 | A **syscall** is a privilege transition ~100× a function call → WHY vDSO / io_uring / avoid-syscalls-in-hot-loops | RECOMPUTED | `_recompute.py` #11; 04 §1 "syscall is … a controlled privilege transition" + A mode-switch cost |
| 12 | `/proc` + `/sys` are kernel-generated virtual filesystems (not disk files); `perf`/eBPF for live tracing | VERIFIED (reuse) | 04 §1 "`/proc` and `/sys` are virtual filesystems generated by kernel code", §1 perf/eBPF |
| 13 | **eBPF verifier** trades expressiveness for safety: proves bounded memory + control flow or rejects | VERIFIED (reuse) | 04 §1/§3 "verifier … conservative static proof; valid but unprovable programs are rejected" |
| 14 | **Multi-level page tables + TLB** (real x86-64 4/5-level) underlie Linux VM (`mmap`, page fault as control flow) | VERIFIED (reuse, via A) | A `_research.md` rung 5 (4-level walk = 400 ns); 04 §1 VM (`mmap`/`/proc/*/maps`/page cache/reclaim) |

## `[UNVERIFIED]` carry-forward (none load-bearing — recomputed or reused from 04's line-cited reads)
- **Linux kernel source/docs + man-pages primary text** — man7.org / kernel.org HTTP **000** this wave.
  Mechanisms reused from 04's line-verified `sched.h`/cgroup-v2/`epoll(7)`/procfs/bpf reads;
  Linux-specific exact structs/flags illustrative until a fetch heals.
- **io_uring exact SQ/CQ ring layout + opcode set** — concept recomputed; byte-level layout
  `[UNVERIFIED]` (carried from 04 §6 as appendix-B topic).
- **Exact CFS/EEVDF constants** (sched weight table, latency-nice, min granularity) and **nice→weight
  table** are version-specific — taught as *mechanism with arithmetic*, not fixed numbers.
- **NUMA topology, Spectre/Meltdown mitigations, transparent huge pages thresholds, real OOM-score
  heuristics** — flagged by 04 §6 as appendix-B depth; structural description only, exact policy
  `[UNVERIFIED]`.
- **Carried from 04 (unchanged):** OSTEP exact text (PDFs blocked), Gregg USE-Method page (blocked),
  xv6 book rev4 Ch.9 title, hardware-dependent syscall/context-switch/TLB-miss absolute numbers.

**0 blockers.** Reference-grade, exercise-free; all numbers re-derived (`_recompute.py` 14/14);
all mechanisms reused from 04's line-verified source reads. Substrate for appendices I (docker) and J
(kubernetes) established (cgroups v2 + namespaces).
